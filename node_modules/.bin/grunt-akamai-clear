#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-akamai-clear/bin/grunt-akamai-clear" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-akamai-clear/bin/grunt-akamai-clear" "$@"
  ret=$?
fi
exit $ret
